licenses(["notice"])

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "xla_ffi_handlers",
    srcs = [
        "src/jax_ffi.cpp",
        "src/extension/json11/json11.cpp",
    ],
    hdrs = glob([
        "src/extension/**/*.h",
        "src/extension/**/*.hpp",
        "src/extension/**/*.inl",
    ]) + ["src/ffi_abi.h"],
    alwayslink = True,
    copts = [
        "-std=c++17",
        "-Wno-attributes",
        "-Wno-return-type",
    ],
    defines = [
        "CUDA_BACKEND=1",
    ],
    includes = [
        "src",
        "src/extension",
    ],
    deps = [
        "@local_config_cuda//cuda:cuda_driver",
        "@local_config_cuda//cuda:cuda_headers",
        "@local_config_cuda//cuda:cuda_runtime",
        "@local_config_cuda//cuda:nvrtc",
        "@xla//xla/ffi/api:c_api",
        "@xla//xla/ffi/api:ffi",
    ],
)

cc_binary(
    name = "libjcn_ffi_openequivariance.so",
    srcs = ["src/pjrt_ffi_provider.cpp"],
    linkopts = ["-Wl,--no-undefined"],
    linkshared = True,
    tags = ["manual"],
    deps = [
        ":xla_ffi_handlers",
        "@xla//xla/pjrt/c:pjrt_c_api_ffi_extension_hdrs",
        "@xla//xla/pjrt/c:pjrt_c_api_hdrs",
    ],
)
